# Bot Command Catalog

Generated by directly introspecting `services/bot/handlers.py`'s real
aiogram `Router` (`python -c "from services.bot.handlers import router; ..."`,
not hand-maintained), so this list cannot silently drift from what the bot
actually runs. Re-run the introspection above after any handler change to
verify this table before trusting it again.

**There are no callback query, inline query, or button-press handlers
anywhere in this codebase today** — every interactive surface is either a
slash command, a plain-text message match, a shared contact, or a photo
upload. `web/miniapp`'s own buttons talk to the gateway's WebSocket API
directly, never through the bot. Sections of the parent directive asking
about "callback/button performance" (inline keyboards) describe a
feature that does not exist in this codebase; nothing was built or
measured for it because there is nothing to measure.

## Registered handlers (message observer), in registration order

Registration order matters: aiogram stops at the first handler whose
filters all pass, so a specific command handler registered before a
catch-all (e.g. `on_agent_portal_command`'s `Command("portal")` before
`on_agent_sms`'s bare `F.text`) is what keeps `/portal` from being
swallowed as a forwarded SMS — `tests/integration/test_bot_handlers.py::
test_portal_command_sends_a_login_link_only_to_an_active_agent` is the
regression test for exactly that.

| # | Handler | Trigger | Description | Admin-configurable? | RBAC/audience | Content source | Rate limit | Measured P50/P95/P99 | Error handling | Audit | Analytics | Test coverage |
|---|---------|---------|-------------|---------------------|----------------|-----------------|------------|----------------------|----------------|-------|-----------|----------------|
| 1 | `cmd_start` | `/start`, incl. `/start ref_<id>` deep link | Registration entry point; welcomes back a returning user with the Mini App keyboard, or starts contact-share registration | No | Everyone | `services/bot/i18n.py` (am/en/om/ti) | None | Not yet measured under real traffic (see below) | Falls through to aiogram's own unhandled-exception logging; no user-facing crash text | No (not a mutating action by itself) | `telegram_commands_total{handler="cmd_start"}` | `tests/integration/test_bot_handlers.py` (multiple) |
| 2 | `on_contact` | Shared contact (`F.contact`) | Completes registration from a shared phone number | No | Everyone (pre-registration) | i18n | None | Not yet measured | `ContactMismatch`/`InvalidPhone`/`PhoneAlreadyRegistered` each get a specific, accurate reply | No | via `telegram_commands_total{handler="on_contact"}` | `test_bot_handlers.py` |
| 3 | `cmd_play` | `/play` | Sends the Mini App launch keyboard | No | Registered users | i18n | None | Not yet measured | Registered-user check only | No | Yes | `test_bot_handlers.py` |
| 4 | `cmd_balance` | `/balance` | Reports cash/bonus/locked balance | No | Registered users | i18n | None | **Measured** (bench script, real local Postgres): old 6-query pattern p50 7.24ms/p95 13.29ms/p99 14.72ms → new single-query p50 1.57ms/p95 2.28ms/p99 2.73ms (DB time only, not full command latency) | Registered-user check only | No | Yes | `test_bot_handlers.py` |
| 5 | `cmd_history` | `/history` | Last 10 finished rounds | No | Registered users | i18n | None | Not yet measured | Registered-user check only | No | Yes | `test_bot_handlers.py` |
| 6 | `cmd_invite` | `/invite` | Referral link + referred-user count | No | Registered users | i18n | None | Not yet measured | Registered-user check only | No | Yes | `test_bot_handlers.py` |
| 7 | `cmd_rules` | `/rules` | Static rules text | No | Everyone | i18n | None | Not yet measured | None needed (no failure mode) | No | Yes | `test_bot_handlers.py` |
| 8 | `cmd_support` | `/support` | Static support contact info | No | Everyone | i18n | None | Not yet measured | None needed | No | Yes | `test_bot_handlers.py` |
| 9 | `cmd_deposit` | `/deposit <amount>` | Starts a Chapa or manual deposit | Partially — provider availability is admin-configurable (`payments:configure`) | Registered users | i18n | Yes — `deposits.DepositRateLimited` | Not yet measured (includes a real payment-provider call; expect NORMAL-class, not FAST) | 7 distinct provider/limit exceptions, each a specific reply | Provider-level (existing payments audit trail) | Yes | `test_bot_handlers.py` |
| 10 | `cmd_withdraw` | `/withdraw <amount> <account> <name>` | Starts a withdrawal | Partially — same as deposit | Registered users | i18n | Implicit via `withdrawals.request_withdrawal`'s own caps | Not yet measured (NORMAL-class expected) | 5 distinct exceptions, each a specific reply | Existing payments audit trail | Yes | `test_bot_handlers.py` |
| 11 | `cmd_limits` | `/limits ...` | Responsible-gaming deposit/loss limits, cool-off, self-exclusion | No | Registered users | i18n | None | Not yet measured | Multiple invalid-input branches, each a specific reply | Via `responsible_gaming` module's own state changes | Yes | `test_bot_handlers.py` |
| 12 | `cmd_language` | `/language <code>` | Sets `users.language` | No (locales are hardcoded to `SUPPORTED_LANGUAGES`) | Everyone | i18n | None | Not yet measured | Rejects unsupported codes | No | Yes | `test_bot_handlers.py` |
| 13 | `cmd_change_username` | `/change_username <name>` | Sets `users.display_name` | No | Registered users | i18n | None | Not yet measured | Length-limit check | No | Yes | `test_bot_handlers.py` |
| 14 | `on_photo` | Photo upload (`F.photo`) | Attaches a receipt image to the latest pending manual deposit | No | Registered users | i18n | None | Not yet measured | "No pending request" reply if none found | No | Yes | `test_bot_handlers.py` |
| 15 | `on_agent_portal_command` | `/portal` (active payment agents only) | Mints a one-time Agent Portal login link | No | `payment_agents.is_active` only | i18n | None | Not yet measured | None needed | No | Yes | `test_bot_handlers.py` |
| 16 | `on_agent_sms` | Any text, active payment agents only | Forwards Telebirr SMS text into `ingest_sms_evidence` | No | `payment_agents.is_active` only | i18n | Implicit via ingestion's own dedup | Not yet measured | 5 distinct ingestion outcomes, each reported | Ingestion's own `payment_evidence` audit trail | Yes | `test_bot_handlers.py` |
| 17 | `on_menu_text` | Any text matching a localized menu button label | Dispatches to the matching slash-command handler | No | Varies by dispatched command | i18n | Inherits dispatched command's | Not yet measured | Falls back to "please register" for unrecognized text | No | No (delegates to the dispatched command's own) | `test_bot_handlers.py` |
| 18 | `on_unhandled_message` | Catch-all | Silent drop (registered) / registration prompt (unregistered) | No | Everyone | i18n | None | Not yet measured | None needed | No | Yes | `test_bot_handlers.py` |

## What "admin-configurable" actually means today

**No command registry exists yet.** Every row above says "No" or
"Partially" because there is currently no data-driven mechanism to
disable a command, rename its description, adjust its cooldown, or gate
it by role from the Admin Dashboard without a code change and a
deployment. This is a real, acknowledged gap against the parent
directive's Section 4 ask ("command registry + command manager"), and it
is **deliberately deferred**, not overlooked: the directive's own
Section 87 priority order places "command registry + command manager" at
#4, after "bot command latency diagnosis" (#2, this document) and
"webhook/worker optimization" (#3, `docs/TELEGRAM_PERFORMANCE.md`) —
both of which this pass completed first, in that order, per the
directive's own explicit instruction not to reverse it. Building a
correct, safe, RBAC-gated, audited command registry (new schema, admin
CRUD screens, content-editor integration, rate-limit enforcement wired
into `perf_middleware`) is a substantial net-new feature in its own
right and was not rushed into this same pass.

## What "Content source" means

Every reply already goes through `services/bot/i18n.py::t()`, with a
real, tested am → en → am fallback chain
(`tests/unit/test_bot_no_hardcoded_strings.py` enforces "no hardcoded
user-facing string anywhere in `handlers.py`"). Bot Content admin
overrides (`services/bot/bot_content_sync.py`, from an earlier pass)
already let an admin edit specific keys without a deploy — this
catalog's "Content source" column doesn't distinguish "file default" from
"admin override" per row because `t()` resolves that transparently and
identically for every command; see `docs/CONTENT_STUDIO.md`-equivalent
prior work if/when a unified Content Studio ships.
